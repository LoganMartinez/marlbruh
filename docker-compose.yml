version: '3'

services:
  marlbruh:
    image: marlbruh
    restart: unless-stopped
    environment:
      - DATA_PATH=/data/
    ports:
      - 8100:8100
    volumes:
      - $HOME/src/marlbruh-docker/data/:/data